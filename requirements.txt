openpyxl>=3.1

# Опционально — только для src/ocr_ingest.py (OCR новых PDF через Claude API,
# см. README "Как скормить программе новый PDF"). Для запуска на приложенных
# к заданию данных (python run.py) не требуется.
# anthropic>=0.40
